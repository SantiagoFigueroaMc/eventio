# TODO List
Complete the following points one by one.
Make sure each point is implemented correctly before moving on to the next.
Mark with an "X" the tasks that are done.

# New features
- [X] Show the count of childrens on each node of the first column
- [X] Add options to copy/cut/paste/delete on the context menu of the nodes in the first column
- [X] Allow to move buttons outside of the image, the buttons shouldnt be cropped
- [X] Add a button to hide the buttons in the second column. If the users clicks it it acts as a toggle, if the user holds it it acts as button that only hides the image buttons while it is pressed. This button should be on the right side of the second column as an eye-slashed icon
- [X] When adding a new button or event in the third column it should keep the focus on the current node. Update the node childrens list.
- [X] Allow to edit child nodes in the third column
- [X] Create a zip with the proyect when sharing. it should include the images. make sure to export in a way that latter the links between page and image can be re-built. 
- [X] Add an import project button in the projects modal. It should allow an import of only text or the zip file with the images. make sure when importing the images that they are linked to the corresponding page.
- [ ] Split main js file into smaller files human readable.
